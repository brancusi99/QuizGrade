<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rezultate Test</title>
    <link rel="stylesheet" href="/main.css">
    <style>
        .results-container {
            max-width: 600px;
            margin: 40px auto;
            padding: 30px;
            background: #fff;
            text-align: center;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            border-top: 8px solid #28a745;
        }
        .results-container h1 {
            color: #333;
        }
        .score {
            font-size: 3em;
            font-weight: bold;
            color: #28a745;
            margin: 20px 0;
        }
        .details {
            font-size: 1.1em;
            color: #555;
        }
        .btn-back {
            display: inline-block;
            margin-top: 30px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <header>
        <img src="/Assets/logo.png" class="logo" alt="QuizGrade Logo">
        <nav>
            <span>Bine ai venit, <%= user.name %>!</span>
            <a href="/users/dashboard">Dashboard</a>
            <a href="/users/logout">Logout</a>
        </nav>
    </header>

    <div class="results-container">
        <h1>Rezultate Test</h1>
        <p>Felicitări pentru finalizarea testului!</p>
        
        <div class="score">
            <%= result.score %>%
        </div>
        
        <div class="details">
            <p>Ai răspuns corect la <strong><%= result.correct %></strong> din <strong><%= result.total %></strong> întrebări.</p>
        </div>

        <a href="/users/dashboard" class="btn-back">Înapoi la Dashboard</a>
    </div>

    <footer>
        <p>&copy; 2025 QuizGrade</p>
    </footer>
</body>
</html>